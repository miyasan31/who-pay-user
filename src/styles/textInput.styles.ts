import { StyleSheet } from "react-native";

export const textInputStyles = StyleSheet.create({
	bg: {
		padding: 12,
		borderRadius: 10,
	},
	half: {
		width: "100%",
		padding: 12,
		borderRadius: 10,
	},
});
